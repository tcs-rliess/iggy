if !pinned
	i.rm-facet-btn.fa.fa-times

span.sublabel #{label}:
ul.subresults
	if selected && selected.length
		each el, idx in selected
			li
				span.txt= el
				i.rm-facet-btn.fa.fa-times

.subselect.closed
.loader
	i.fa.fa-cog.fa-spin
