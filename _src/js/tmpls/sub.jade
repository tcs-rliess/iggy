if !pinned
	.rm-facet-btn.fa.fa-remove

span.sublabel #{label}:
ul.subresults
	if selected && selected.length
		each el, idx in selected
			li
				span.txt= el
				i.rm-facet-btn.fa.fa-remove

.subselect.closed
.loader
	i.fa.fa-cog.fa-spin
